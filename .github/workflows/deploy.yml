name: Deploy

on:
  push:
    branches: [main]
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  build:
    uses: jeffreycwitt/loyola-jekyll-cms-template/.github/workflows/reusable.yml@main
    with:
      theme: academic
    secrets: inherit
